{
  "name": "assignment---1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
   "scripts": {
    "sample-test": "npx playwright test",
    "delete-report": "if exist allure-results rmdir /s /q allure-results & if exist allure-report rmdir /s /q allure-report",
    "generate-report": "npx allure generate allure-results --clean -o allure-report",
    "serve-report": "npx allure open allure-results",
    "test": "npm run delete-report && npm run sample-test  &&  npm run generate-report && npm run serve-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.5.1",
    "allure": "^3.17.0",
    "allure-commandline": "^2.46.1",
    "allure-playwright": "^3.12.2"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
